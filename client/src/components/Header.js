function Header() {
  return (
    <header
      style={{
        background: "#007bff",
        padding: "10px 0",
        color: "white",
        textAlign: "center",
      }}
    >
      <h1>Task Manager</h1>
    </header>
  );
}

export default Header;
